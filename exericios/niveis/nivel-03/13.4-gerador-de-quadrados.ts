/*
    4 - Gerador de Quadrados

    Objetivo: Criar uma representação visual de um quadrado no console usando caracteres.

    Pseudo-Código:

    1 - Definir uma variável tamanho (ex: 5);

    2 - Criar um loop externo que representa as linhas;

    3 - Dentro dele, criar um loop interno que representa as colunas;

    4 - Concatenar um caractere (ex: "*") em uma string até completar a linha;

    5 - Imprimir a linha e repetir até o fim do loop externo.
*/

const tamanho: number = 5;

for (let )